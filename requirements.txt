# Core detection + tracking
ultralytics>=8.4.0
torch>=2.0.0
torchvision>=0.15.0

# supervision provides ByteTrack, DetectionsSmoother and LineZone.
# Pinned tightly: LineZone.line became LineZone.vector in 0.30.x
supervision==0.30.2

# Video / image / IO
opencv-python>=4.8.0
numpy>=1.24.0

# Web server
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6
websockets>=12.0

# HuggingFace (model downloads)
huggingface_hub>=0.17.0
